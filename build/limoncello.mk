# Makefile variables specific to the limoncello laptop
# environment variables on this machine are not managed by environment modules, instead all binaries
# and libraries are installed through the system package management utility in the standard location
FC = h5pfc
INCLUDEPATH =
LIBS =

